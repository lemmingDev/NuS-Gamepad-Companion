---
description: Run the full static gate (flutter analyze + flutter test) and report.
---

In `C:\Users\PPSHS VR\Downloads\NuS-Gamepad-Companion`, run `flutter analyze`
then `flutter test`. Report: analyzer issues (must be zero) and the test
tally (all must pass). If anything fails, show the failing output verbatim
and stop — do not proceed to build or install.
