<!--
  NuS-Gamepad-Companion — Android setup snippets.
  Generated by `flutter create`; merge the blocks below into:
    android/app/src/main/AndroidManifest.xml
    android/app/build.gradle(.kts)

  See SETUP.md for the full steps.
-->

<!-- ============ AndroidManifest.xml (inside <manifest>, outside <application>) ============ -->
<!--
<uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

<uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
-->
<!-- Location is only used for BLE scanning on Android 11 and below (API <= 30).
     Requested at runtime alongside the Bluetooth permissions; safe to keep declared. -->
<!--
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
-->

<!-- ============ android/app/build.gradle(.kts) ============ -->
<!--
android {
    // flutter_blue_plus requires minSdk 21+; 24 is the realistic floor.
    defaultConfig {
        minSdk = 24
    }
}
-->
